import React from 'react'
import { Text, View } from 'react-native'
import { styles } from '../theme/appTheme';

export const Settings = () => {
  return (
    <View>
        <Text style={styles.title}> Hola desde Settings</Text>
    </View>
  )
}
